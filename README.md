## Notes App

A full-stack notes application with user authentication, per-user note CRUD with rich text editing, structured logging, global exception handling, unit testing, and static code quality analysis.

**Stack:** Express.js + Prisma + Neon PostgreSQL (backend), React + Vite + TypeScript (frontend).